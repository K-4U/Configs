\ProvidesPackage{style}
\usepackage[a4paper]{geometry}
\usepackage[dutch]{babel}
\usepackage{fullpage} 
\usepackage{xr-hyper}
\usepackage{xcolor}
%Defining that color
\definecolor{bl}{rgb}{0.0,0.2,0.6} 
\usepackage{bookmark} %To have pdftex generate bookmarks in PDF's
\hypersetup{pdfborder={0 0 0}} %Set hyperlink borders to 0
%Options: Sonny, Lenny, Glenn, Conny, Rejne, Bjarne, Bjornstrup
\usepackage[Bjornstrup]{fncychap} %Chapter stuff
\usepackage{hyperref} %For hyperlinks


\renewcommand{\familydefault}{\sfdefault} %Other font

%For code highlighting
\usepackage{minted}

%Colors in the section


\usepackage{sectsty}
\usepackage[compact]{titlesec} 
%\allsectionsfont{\color{bl}\scshape\selectfont}
%End of section-colors

%For receiving the last page number
\usepackage{lastpage} 
%For the footers and headers
\usepackage{fancyhdr}
%To allow the footers and headers
\pagestyle{fancy}

%Clear footers and headers
\fancyhead{}
\fancyfoot{}
%The hight of both is 12pt
\headheight12pt
\headsep12pt

%For adjusting the text in the headers
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\sectionmark}[1]{\markright{\thesection.\ #1}}
\renewcommand{\chaptermark}[1]{%
	\markboth{\bf \thechapter.\ #1}{}
}


%Setting the headers
\fancyhead[LE,RO]{\slshape \rightmark}
\fancyhead[LO,RE]{\slshape \leftmark}

%Setting the footers
\makeatletter
%Left footer
\fancyfoot[RE, LO] {\@title\ - \@author\ (\@studentNumber)\ - \@class \\ \@institute}
%Right footer
\fancyfoot[RO, LE] {\thepage\ of \pageref{LastPage}}

%This is for pages where the pagestyle is plain
%For example, pages with a chapter
\fancypagestyle{plain}{%
\fancyhf{} % clear all header and footer fields
\fancyfoot[RE, LO] {\@title\ - \@author\ (\@studentNumber)\ - \@class \\ \@institute}
\fancyfoot[RO, LE] {\thepage\ of \pageref{LastPage}}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0.4pt}}

\makeatother
\renewcommand{\footrulewidth}{0.4pt} 


%TITLE PAGE
\makeatletter
\newcommand\education[1]{\renewcommand\@education{#1}}
\def\@education{\@latex@error{No \noexpand\education given}\@ehc}

\newcommand\studentNumber[1]{\renewcommand\@studentNumber{#1}}
\def\@studentNumber{\@latex@error{No \noexpand\studentNumber given}\@ehc}

\newcommand\institute[1]{\renewcommand\@institute{#1}}
\def\@institute{\@latex@error{No \noexpand\institute given}\@ehc}

\newcommand\eduYear[1]{\renewcommand\@eduYear{#1}}
\def\@eduYear{\@latex@error{No \noexpand\eduYear given}\@ehc}

\newcommand\tutor[1]{\renewcommand\@tutor{#1}}
\def\@tutor{\@latex@error{No \noexpand\tutor given}\@ehc}

\newcommand\location[1]{\renewcommand\@location{#1}}
\def\@location{\@latex@error{No \noexpand\location given}\@ehc}

\newcommand\course[1]{\renewcommand\@course{#1}}
\def\@course{\@latex@error{No \noexpand\course given}\@ehc}

\newcommand\class[1]{\renewcommand\@class{#1}}
\def\@class{\@latex@error{No \noexpand\class given}\@ehc}

%If you want to add a variable, do it here
%\newcommand\variable[1]{\renewcommand\@variable{#1}}
%\def\@variable{\@latex@error{No \noexpand\variable given}\@ehc}
\makeatother


\makeatletter
%Title page command
\def\maketitle{%
	\begin{titlepage}
	\pagenumbering{alph}
  \null
  \thispagestyle{empty}%
  \vfill
  \begin{center}\leavevmode
    \normalfont
    {\huge \@title\par}%
    %\vskip 1cm
    \vfil
    {\Large \@author (\@studentNumber) \par}%
    \vskip 1cm
    \begin{tabular}{|l l|}
    	\hline
    	{\bf Document titel} & \@title \\ \hline
    	{\bf Auteur} & \@author (\@studentNumber) \\ \hline
    	{\bf Klas} & \@class \\ \hline
    	{\bf Opleiding} & \@education \\ \hline
    	{\bf School} & \@institute \\ \hline
    	{\bf Leerjaar} & \@eduYear \\ \hline
    	{\bf Beoordelend docent} & \@tutor \\ \hline
    	{\bf Vakcode} & \@course \\ \hline
    	{\bf Datum} & \@date \\ \hline
    	{\bf Plaats} & \@location \\ \hline
    \end{tabular}
  \end{center}%
  \vfill
  \null
  \end{titlepage}
  \cleardoublepage\pagenumbering{arabic}
  }
\makeatother
%END OF TITLE PAGE


%This is for making the chapter headings a bit smalled and also a bit more to
%the top of the page
\makeatletter
\renewcommand*{\@makechapterhead}[1]{%
  \vspace*{5\p@}%
  {\parindent \z@ \raggedright \normalfont
    \ifnum \c@secnumdepth >\m@ne
      \if@mainmatter%%%%% Fix for frontmatter, mainmatter, and backmatter 040920
        \DOCH
      \fi
    \fi
    \interlinepenalty\@M
    \if@mainmatter%%%%% Fix for frontmatter, mainmatter, and backmatter 060424
      \DOTI{#1}%
    \else%
      \DOTIS{#1}%
    \fi
  }}
% For the case \chapter*:
\renewcommand*{\@makeschapterhead}[1]{%
  \vspace*{5\p@}%
  {\parindent \z@ \raggedright
    \normalfont
    \interlinepenalty\@M
    \DOTIS{#1}
    \vskip 5\p@
  }}
\makeatother
